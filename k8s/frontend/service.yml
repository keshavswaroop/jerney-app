apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: jerney

spec:
  selector:
    app: frontend
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30036
