{{- with .Values.backend}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .serviceName }}
  namespace: {{ $.Values.namespace }}

spec:
  selector:
    app: {{ .labels.app }}
  ports:
    - port: {{ .image.port }}
      targetPort: {{ .image.containerPort }}
{{- end}}